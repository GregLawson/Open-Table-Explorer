<%= form_for(@huelshow) do |f| %>
  <% if @huelshow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@huelshow.errors.count, "error") %> prohibited this huelshow from being saved:</h2>

      <ul>
      <% @huelshow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% semantic_form_for @huelshow do |form| %>
    <%= form.inputs %>
    <%= form.buttons %>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :shortname %><br />
    <%= f.text_field :shortname %>
  </div>
  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
